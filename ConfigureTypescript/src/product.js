"use strict";
const watercolor = {
    name: "watercolor paint tubes",
    price: 400,
    quantity: 1,
};
const printProduct = (product) => console.log(`${product.name}--${product.price}`);
printProduct(watercolor);
